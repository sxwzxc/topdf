# 2026-06-30

## 大图片 500 错误修复 — 全链路压缩方案

**问题**：上传大分辨率/大文件体积图片时，后端返回 500 (Internal Error)。根源是：
- 前端无压缩，原始文件直传
- ImageEditor 输出未压缩 PNG（裁剪/旋转/矫正后可能很大）
- 后端无尺寸限制，Pillow `DecompressionBombError` 或内存溢出

**修复 (三端加固)**：

### 1. 新增 `src/lib/imageCompressor.ts`
- 最长边 > 4096px → 等比缩放到 4096px
- PNG/WebP → 转 JPEG (quality 85%)
- 已合规的 JPEG → 跳过避免二次损失
- GIF → 保持原样（超大则拒绝）
- 单张 > 50MB → 拒绝并提示
- 导出 `compressImage(file)`, `compressBlob(blob, name)`, `compressImages(files)`, `ImageTooLargeError`, `formatFileSize`

### 2. 修改 `src/app/page.tsx`
- `addFiles` 改为 async，逐张调用 `compressImage`
- 添加 `compressing` / `compressingCount` / `applyingEdit` 状态
- 压缩期间上传区显示 spinner + 进度 "(d/total)"
- 压缩期间禁用拖拽、点击、清空、转换按钮
- `applyEdit` 改为 async，调用 `compressBlob` 压缩编辑器输出
- `handleDrop` / `handleFileChange` 加压缩中守卫

### 3. 修改 `cloud-functions/api/img2pdf.py`
- `MAX_DIMENSION = 4096`：超限自动 LANCZOS 缩放
- `MAX_BODY_SIZE = 50MB`：超限返回 413
- `Image.MAX_IMAGE_PIXELS = 200_000_000`：提高 Pillow 解压阈值
- 捕获 `DecompressionBombError` → 400 + 中文提示
- 捕获 `MemoryError` → 400 + 中文提示
- 捕获 `OSError` → 400 + 中文提示
- `ValueError` → 400，其余 `Exception` → 500

## 客户端 PDF 合成按钮

新增纯客户端 PDF 合成能力，不经过后端，零依赖。

### 新增 `src/lib/clientPdfBuilder.ts`
- `buildAndDownloadPdf(files, filename)` — 加载图片 → Canvas → JPEG → 手动构建 PDF 1.4 → 触发下载
- `buildPdfBlob(files)` — 同上但不触发下载，返回 Blob
- PDF 结构与 Python 后端完全一致：catalog / pages / content stream / DCTDecode JPEG XObject / xref
- 完全在浏览器本地执行，不受后端文件大小限制

### 修改 `src/app/page.tsx`
- 新增「客户端合成 PDF」按钮，位于「转为 PDF」按钮下方
- `handleClientConvert`：取 images 中的 File 对象直接传给 buildAndDownloadPdf
- 处理期间禁用所有操作按钮，显示"合成中..."
- UI 风格与主按钮区分（暗色/次色），Monitor 图标

## Pillow DecompressionBombError 导入兼容修复

EdgeOne 运行时 Pillow 版本较旧，`from PIL import DecompressionBombError` 报错。
改为 try/except 导入：优先 `from PIL.Image import DecompressionBombError`，
失败则回退为 `ValueError`（DecompressionBombError 的父类）。

## 四点矫正算法重写 — 仿射 → 透视变换 (Homography)

**原算法**：双三角形仿射近似，无法表达透视消失点。

**第一版 Homography（已废弃）**：幂迭代 DLT + 矩阵求逆。bug：列主序存储/行主序索引错乱 + 幂迭代数值不稳定。

**第二版（当前）**：对标 OpenCV getPerspectiveTransform + warpPerspective
- DLT 直接解 8×8 线性系统（高斯消元 + 列主元）
- 矩阵符号与 marrotte/projective 开源库对齐：`[x,y,1,...,-x*u,-y*u | u]`
- Hinv = 3×3 解析求逆，再对每个 dst(u,v) 做 Hinv·[u,v,1]ᵀ 反向映射 + 双线性插值
- 输出尺寸改用 max 替代 avg，避免裁切内容
- **自动精确裁剪**：矫正后从四边向中心扫描，找到非白像素边界框，裁掉白边 + 2px 安全边距
- 新增 `findContentBounds` 函数（RGB>250 判为白色）
- 移动端触控：handle 28px / touch-action:none / passive:false / setPointerCapture

## 图片色彩优化勾选框

- 新增 `src/lib/imageEnhancer.ts` — 独立图片增强模块
  - `enhanceImageFile(file)` → 白平衡 + 直方图拉伸 (contrast 0.7) + Unsharp Mask 锐化 (0.4) → 返回增强 PNG File
- `page.tsx` 操作按钮上方添加「优化图片色彩」开关（✨ 图标 toggle）
- 勾选后，两个转换路径都先增强再合成：
  - 服务端转换：`enhanceImageFile` → 增强后的 File → FormData → POST /api/img2pdf
  - 客户端合成：`enhanceImageFile` → 增强后的 File → buildAndDownloadPdf

## 色彩修正 / 文档增强功能

ImageEditor 新增「色彩修正」Tab，将手机拍照转为类扫描文档效果。

### 图像处理流水线
1. **白平衡 + 直方图拉伸**：每 RGB 通道独立取 2%~98% 分位，线性拉伸到 0~255（contrast 参数调分位范围）
2. **亮度偏移**：±40% 范围，0.5 = 不调整
3. **Unsharp Mask 锐化**：3×3 高斯模糊核近似，`sharp = orig + (orig - blur) * strength`
4. **灰度转换**：亮度加权 (0.299R + 0.587G + 0.114B)

### UI
- 上/下分屏预览：原图 vs 增强后
- 四组控制器：黑白切换 · 对比度 · 锐化 · 亮度（各 slider + 数值）
- 参数变化 200ms 防抖后实时更新预览

### 文件变更
- `src/components/ImageEditor.tsx`
  - Mode 增加 "enhance"
  - 新增 EnhancePanel 组件 + EnhancePanelProps
  - 新增 enhanceDocument / autoWhiteBalance / unsharpMask / adjustBrightness / toGrayscale

## 老年人友好大字号 + 光效动画毛玻璃

### 尺寸升级
- `html { font-size: 17px }`（比浏览器默认大一号）
- 容器 `max-w-3xl`、间距 `gap-3/4/5`、按钮 `h-14`（主）/ `h-13`（次）
- Hero 标题 `text-3xl sm:text-4xl`、卡片内字体普遍 `text-base`
- 上传图标 `w-14 h-14`、序号标识 `w-6 h-6`、操作按钮 `w-6 h-6`
- 编辑器滑块 thumb `22px`、比例芯片 `text-[13px]`、按钮 `min-w-[36px] h-[36px]`
- 滚动条加宽到 `8px`

### 光效系统
- 3 个固定漂浮光球（蓝 + 琥珀 + 混合），`orb-float` 12s 循环动画
- 全局点阵纹理 `dot-pattern`（32px 网格径向渐变圆点）
- 主卡片 `glass-card`：毛玻璃 `blur(20px) saturate(160%)`
- 主按钮光泽扫过 `::after` 伪元素 + hover 时 `translateX`
- Dropzone 激活态 scale + 内发光 + 外环光晕
- 缩略图卡片 hover 上浮 `translateY(-3px)` + 蓝光阴影

### 动画增强
- Stagger 子元素入场扩展至 8 级延时
- 新增 `scale-in` 动画（缩放 + 淡入）
- 新增 `shimmer-loading` 加载骨架闪烁
- 光球 `orb-float` 独立动画相位（-0s / -4s / -8s 偏移）
- 编辑器面板 0.3s 缓入、阴影加剧

## 淡雅色彩 + 层次立体感 + 署名

### 色彩系统
- 新增 4 组淡雅色值变量：`--pastel-pink/mint/lavender/peach`
- 3 个漂浮光球改为多色混合渐变（蓝紫 / 琥珀粉 / 绿蓝）
- Dropzone 上传区背景改为薰衣草→薄荷→粉红三色渐变
- 3 个特性卡片各配独立淡雅底色 + 对应图标色：
  - 拖拽上传：薰衣草紫 + 紫色图标
  - 裁剪矫正：薄荷绿 + 翠绿图标
  - 一键导出：粉红 + 玫瑰图标

### 立体层次
- 阴影改为双层叠加：`shadow-sm/md/lg/xl` 各含 2 层
- 毛玻璃卡片顶部增加 1px 高光线（模拟光源从上方）
- 实色卡片底部渐变条 hover 展开动画
- 新增 `card-tint-*` 类：蓝/薄荷/薰衣草/蜜桃/粉 5 种淡彩卡片

### 署名
- 导航栏右侧 GitHub 图标旁增加「made by **sxw**」，移动端隐藏

## 暗色 → 亮色主题重构 + 高级感设计

### 全局 CSS 重写 (`globals.css`)
- 调色板：`#f8fafc` 背景 / `#ffffff` 卡片 / `#3b82f6` 品牌蓝 / `#f59e0b` 暖色点缀
- body::before 固定径向渐变光晕（蓝+琥珀），营造氛围光
- 精细阴影系统：`sm/md/lg/glow` 层级
- 毛玻璃导航栏：`backdrop-blur saturate(180%)`
- 按钮渐变 + hover 发光阴影 + translateY 微动
- Dropzone：双色渐变 + hover 外发光 ring
- 缩略图卡片：`thumb-item` 配合 hover 上浮 + 拖拽过场动画
- 自定义滚动条、响应式 hover 降级
- Stagger 子元素入场动画（逐项 50ms 延迟）
- 编辑器滑块、裁剪框覆盖样式同步改亮

### 页面重构 (`page.tsx`)
- 新增 Hero 区域：粗体标题 + 副标题
- 新增三个特性卡片行（拖拽上传 / 裁剪矫正 / 一键导出）
- 新增「免费在线工具」✨ 标签
- 缩略图网格：白色圆角卡片 + 磨砂序号 + 白色悬浮操作按钮
- 按钮：主按钮蓝渐变 + 蓝色阴影光晕；次按钮纯白边框
- 颜色错误、API 区域、文件路由、页脚全部亮色化
- 移除多余图标导入（GripVertical, Route, Layers）
